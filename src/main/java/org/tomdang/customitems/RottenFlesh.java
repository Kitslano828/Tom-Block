package org.tomdang.customitems;

import org.bukkit.Material;
import org.tomdang.customitemframework.CustomItem;
import org.tomdang.customitemframework.ItemCategory;
import org.tomdang.customitemframework.Rarity;

public class RottenFlesh extends CustomItem {
	public RottenFlesh(String id, Material material, String displayName, Rarity rarity, ItemCategory itemCategory) {
		super(id, material, displayName, rarity, itemCategory);
	}
}
